<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <title>Sign Up | Members Only</title>
</head>

<body>
  <%- include('partials/header.ejs', {user}) %>
  <div class="sign-up-wrapper">
    <form action="" method="POST" class="sign-up">
      <div>
        <h1>Join us today!</h1>
        <p class="subtitle">Required fields are marked with *</p>
        <% if (errors) { %>
        <% errors.forEach(elem => { %>
        <p class="error"><%= elem.msg %> </p>
        <% }) %>
        <% } %>
      </div>
      <div class="form-section">
        <label for="firstName">*First Name</label>
        <input type="text" name="firstName" id="firstName" value="<%= firstName %>" placeholder="John" required>
      </div>
      <div class="form-section">
        <label for="lastName">Last Name</label>
        <input type="text" name="lastName" id="lastName" value="<%= lastName %>" placeholder="Doe">
      </div>
      <div class="form-section">
        <label for="email">*Email</label>
        <input type="email" name="email" id="email" value="<%= email %>" placeholder="example@gmail.com" required>
      </div>
      <div class="form-section">
        <label for="password">*Password</label>
        <input type="password" name="password" id="password" value="<%= password %>" required>
      </div>
      <div class="form-section">
        <label for="passwordConfirm">*Confirm Password</label>
        <input type="password" name="passwordConfirm" id="passwordConfirm" value="<%= passwordConfirm %>" required>
      </div>
      <p>Already have an account? <a href="/sign-in">Sign in</a></p>
      <button class="submit">Sign Up</button>
    </form>
  </div>
</body>

</html>